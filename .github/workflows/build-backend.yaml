name: Build Backend Image

on:
  push:
    branches:
      - main
    paths:
      - 'rill-backend/**'
      - '.github/workflows/build-backend.yaml'
  pull_request:
    paths:
      - 'rill-backend/**'
      - '.github/workflows/build-backend.yaml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: eseslabs/rill-backend
  PLATFORMS: linux/amd64

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=raw,value=sha-${{ github.sha }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./rill-backend/Dockerfile
          platforms: ${{ env.PLATFORMS }}
          push: ${{ github.event_name != 'pull_request' && github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=rill-backend
          cache-to: type=gha,mode=max,scope=rill-backend

      # After the new image is in GHCR, ping Dokploy to pull + redeploy. The webhook URL carries a
      # deploy token, so it lives in the DOKPLOY_DEPLOY_WEBHOOK repo secret — never hardcoded here.
      # Same gate as the push above: only a real main build triggers a deploy (not PRs).
      - name: Trigger Dokploy deploy
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        run: curl -fsS -X POST "${{ secrets.DOKPLOY_DEPLOY_WEBHOOK }}"
